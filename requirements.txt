# v0.2 依赖（从简）。
# Agent loop 选型确认（2026-08-01）：PydanticAI 单次结构化调用（非 Claude Agent SDK）。
# 模型走百炼兼容端点（Anthropic 协议）；端点+模型名从 config.yaml 读，API key 从 env。

PyYAML>=6.0
pytest>=7.0
pydantic>=2.0
pydantic-ai
pydantic-evals
anthropic
# 重构预备（2026-08-03）：entry_loop 状态机 → OpenAI Agents SDK agent loop；模型 glm-5.2 → deepseek-v4-flash（百炼）。
# pydantic-ai 暂留（重构时再删 + 删 llm.py）；ticker_resolver / dialogue 会被 agent loop 替换，Bug #3/#4 不修。
openai-agents
# W2 录入 loop 承载层（localhost 单页）：FastAPI + uvicorn（作者 2026-08-02 定）
fastapi
uvicorn
# W2 §2.3 核对 agent：SEC EDGAR fetcher（从 pre-market-briefing 搬，用 requests）
requests
