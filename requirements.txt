# v0.2 依赖（从简）。Phase 5（2026-08-04）：pydantic-ai 已删，全栈 OpenAI Agents SDK。
# 模型走百炼兼容端点（OpenAI chat_completions 协议，deepseek-v4-flash）；端点+模型名从 config.yaml 读，API key 从 env。

PyYAML>=6.0
pytest>=7.0
pydantic>=2.0
# OpenAI Agents SDK agent loop（录入 orchestrator + 核对 check_agent，Phase 0-5）。
openai-agents==0.19.2
openai              # AsyncOpenAI client（orchestrator/check_agent 直接 import；openai-agents 传递依赖，显式列）
# 录入 loop 承载层（localhost 单页）：FastAPI + uvicorn（作者 2026-08-02 定）
fastapi
uvicorn
# 核对 agent：SEC EDGAR fetcher（从 pre-market-briefing 搬，用 requests）
requests
# 行情数据源：YahooPriceFetcher（Stage 2 安全边际监控；yfinance 取 regularMarketPrice / 52w 高低 / marketCap）
yfinance
